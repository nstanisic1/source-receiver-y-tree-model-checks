Table 6 summarizes the adversarial source-receiver sensitivity stress test. The table is generated from the audited Figure 4 adversarial sensitivity source bundle and expresses branch-resolution layers, ambiguity handling, and source masks with descriptive labels. Versioned table fields that use sensitivity terminology refer to predefined mask-choice sensitivity states retained for computational traceability.

Weak directional compatibility means source adequacy above one and negative diversity contrast. This is not treated as load-bearing evidence by itself. Matched-envelope entry additionally requires PH908 to enter the state-matched R1a-comparator envelope defined by the R-Z280 and R-M458 comparators under the same branch-resolution, ambiguity-handling, and source-mask configuration. Positive-control status indicates whether the matched R1a comparators recover the expected state under the same configuration.

The ten tested adversarial states all satisfy weak directional compatibility, but none enters the state-matched R1a-comparator envelope. Six states are positive-control-passing and four are not. The control-failing states are retained for transparency but are not treated as load-bearing evidence. These adversarial sensitivity states should not be interpreted as evidence of geographic origin, ethnic identity, population continuity, or migration route.
