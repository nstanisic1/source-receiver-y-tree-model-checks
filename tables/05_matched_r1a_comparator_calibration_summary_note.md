Table 5 summarizes the R1a-comparator-envelope calibration and robustness evidence used before the adversarial sensitivity analysis. It is generated from the audited Figure 3 specificity/calibration source bundle rather than copied from final plotting output.

Margins, negative-control rows, PH908 rows, T95 thresholds, and matched-block permutation procedures are defined by the predefined PH908-versus-negative-control comparison framework. The matched-block permutation is restricted to the predefined matched-comparator calibration grid; label exchangeability is limited to the null that held-out and negative-control rows share the same source-receiver margin distribution within those blocks. Empirical negative-control flag rates are calibration-set quantities and should not be interpreted as population-level false-positive rates. Complete observed margin separation and AUROC values summarize the tested comparison grid only.

The preferred empirical nearest-rank T95 calibration is reported because it reduces observed negative-control flagging to 6/136 while preserving PH908 recovery at 136/136. This supports the PH908/matched R1a comparator calibration contrast under the tested framework and does not identify geographic origin, ethnic identity, population continuity, or migration route.
