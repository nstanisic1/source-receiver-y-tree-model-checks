Table 33 reports the country-code membership of the scored source and receiver masks used in the focused benchmark audit cards. Each scored row is contract-specific: a country code appears only when it contributes to the relevant lineage root and mask in the audited country-code recode used for the core YFull source bundle. The source and receiver row counts therefore reconcile to the country-count fields in Table 26.

The table distinguishes scored focused masks from boundary/no-call contexts. H-M82 and E-M2 are listed only as boundary contexts because diaspora structure, source visibility, or source-receiver independence prevents a load-bearing primary decision. These rows should not be read as numeric source-receiver contracts.

The Arabia/West Asia label used for J-P58 is implemented as the fine Arabia source mask in the scored benchmark card; broader West Asia is contextual language and is not silently substituted into the Table 26 source-country count. The Southeast Asia receiver mask for O-M119 and O-M122 includes both `southeast_asia` and `island_southeast_asia` roll-up values, which is why Taiwan appears where present in the scored root-specific surface.

The ambiguous or corridor-adjacent country-code section records the conservative exclusion rule used for sensitivity checks. These rules address country-level labels that span multiple geographic contexts or are diaspora-heavy. They are sensitivity rules, not historical claims.

Table 33 is a source-receiver mask trace. It should not be interpreted as subnational placement, geographic origin, ethnic identity, population continuity, migration route, ancient-DNA evidence, or population frequency.
