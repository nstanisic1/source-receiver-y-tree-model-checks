Table 17 documents the log-base audit for the diversity contrast D. It is generated by `scripts/08_generate_parser_and_log_base_quality_controls.py`. The manuscript defines D as ln(q2_receiver/q2_source), and the reproducibility pipeline verifies this convention before rebuilding the plotted coordinates.

Rows labelled as headline q2-component recomputation rows include source q2 and receiver q2 values and directly test whether the reported D matches the natural-log recomputation. Rows labelled as adversarial sensitivity D context rows are included to document the log-base convention for versioned sensitivity-state D values, but the source rows do not contain q2 components; those rows are therefore context checks rather than independent q2-component recomputations.

The headline q2-component recomputation rows are audit contexts used to verify the natural-log convention. They are not the primary manuscript coordinate source and should not be used to replace the final benchmark or figure-source coordinates.

The log10 column is included only as a reference contrast for the audit. It is not the reported natural-log convention. This table verifies numerical convention and reporting consistency only; it does not provide evidence of geographic origin, ethnic identity, population continuity, or migration route.
