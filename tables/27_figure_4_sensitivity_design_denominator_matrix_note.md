Table 27 records the Figure 4 sensitivity-design and denominator matrix. It defines what each Figure 4 panel is allowed to claim from the audited source bundle.

Panel A uses ten predefined PH908 alternative-mask states. Weak directional compatibility requires `A_s > 1` and `D < 0`, but it is not load-bearing unless PH908 also enters the state-matched R1a-comparator envelope and the matched R1a positive controls recover under the same state definition.

Panel B is a source-visibility burden calculation. It reports the source-adequacy multiplier still needed for PH908 to reach the matched R1a-comparator source-adequacy threshold. This is not a bias correction, population-size estimate, or origin inference.

Panel C is the adversarial decision gate. It separates weak directional compatibility, matched-envelope failure, positive-control-passing states, and transparency-only control-failing states.

Panel D is supported by two distinct sensitivity layers. The equal-n rarefaction trace reports adequacy and denominator behaviour in the boundary-by-rarefaction grid. The synthetic visibility-degradation trace uses matched R1a positive comparators, applies deterministic source-terminal and source-country thinning together with audited branch-layer collapse, and recomputes `A_s`, `D`, country-visibility gates and decision state. The synthetic degradation controls are operating-characteristic tests under biased observation, not population-size estimates.

This design matrix is a claim-boundary table. It does not provide evidence of PH908 geographic origin, ethnic identity, population continuity, chronology, migration route, ancient-DNA confirmation, population-level probability, or population size.
