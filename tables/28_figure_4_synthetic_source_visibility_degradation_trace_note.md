Table 28 records the synthetic visibility-degradation trace used for Figure 4. It is generated by `scripts/07_generate_figure4_rarefaction_and_degradation.py` from the matched R1a branch-unit rows, the benchmark audit-card matrix, and the retained-visibility rule inputs in `source_data/core_figure4_rarefaction_degradation`.

The experiment degrades the matched R1a positive comparators, R-Z280 and R-M458, under deterministic source-terminal and source-country thinning while evaluating the audited branch-layer collapses. For each retained-visibility fraction and branch-resolution layer, the table recomputes synthetic source adequacy as `synthetic_source_terminal_count_continuous / baseline_receiver_terminal_count`, carries the corresponding branch-layer diversity contrast `D`, applies source and receiver country-visibility gates, and reports the resulting compatibility state.

The table also records two exact thresholds for each comparator: the retained source-visibility fraction at which synthetic `A_s` reaches the `A_s = 1` source-adequacy gate, and the retained source-visibility fraction at which synthetic `A_s` reaches the held-out PH908 reference value.

This is a deterministic operating-characteristic degradation control. It does not estimate historical population size, unsampled individuals, or unobserved historical lineages. It does not replace rarefaction intervals or geographic permutation tests. It should not be interpreted as evidence of geographic origin, ethnic identity, population continuity, chronology, migration route, ancient-DNA confirmation, population-level probability, or population size.
