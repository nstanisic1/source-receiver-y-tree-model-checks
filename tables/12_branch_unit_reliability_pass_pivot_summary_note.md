Table 12 summarizes the compact branch-unit pass pattern for the audit cards. It is derived from the pivot source table and cross-referenced to the row-level matrix in Table 11.

Pass, fail, and no-call have the same meaning as in Table 11. The compact summary is intended for compact interpretation only; row-level counts, branch counts, source-adequacy values, diversity-contrast values, and sensitivity summaries are provided in Table 11.

This table distinguishes robust source-receiver-compatible comparators, stable PH908 model rejection, supporting non-target comparisons, and support-only boundary diagnostics. Versioned source-receiver labels refer to the tested downstream source-receiver geometry. The table does not identify exact origins, ethnic identity, population continuity, or migration route.
