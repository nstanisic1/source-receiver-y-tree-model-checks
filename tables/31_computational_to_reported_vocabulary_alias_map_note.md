Table 31 is a terminology concordance for source-data and reported vocabulary.

The repository identity is source-receiver model checking. Some source-data files, versioned computational outputs, and provenance records retain field names such as `source_wave`, `contract`, or `sensitivity_state` because those labels are part of existing field names, manifests, or hashed source tables. These terms should not be globally rewritten when doing so would break traceability.

The alias map defines how versioned computational terms should be read in reported text and figure captions. It is a terminology concordance, not an analysis table. It should not be interpreted as evidence of geographic origin, ethnic identity, population continuity, migration route, ancient-DNA confirmation, or population-level probability.
